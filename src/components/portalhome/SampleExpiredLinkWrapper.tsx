import React from 'react';
import SampleExpiredLink from './SampleExpiredLink';

const SampleExpiredLinkWrapper: React.FC = () => {
  return <SampleExpiredLink />;
};

export default SampleExpiredLinkWrapper;
